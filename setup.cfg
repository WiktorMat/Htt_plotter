[metadata]
name = htt-plotter
version = 0.0.0
description = Htt_plotter (streaming + logging)

[options]
package_dir =
    = source
packages = find:
python_requires = >=3.9
install_requires =
    numpy
    pandas
    pyarrow
    PyYAML
    matplotlib

[options.packages.find]
where = source
