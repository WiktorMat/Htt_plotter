[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
    "combine: integration tests that run Combine (standalone container or CMSSW cmsenv)",
]
addopts = "-m 'not combine'"
